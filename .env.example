# OpenAI API key — passed through to the Gamaliel API (BYOK, used per request only)
OPENAI_API_KEY=sk-...

# Optional: model id (default: gpt-4.1-mini). Use anthropic/<model> with an Anthropic key.
# GAMALIEL_MODEL=gpt-4.1-mini
